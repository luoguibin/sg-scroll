```
移动端自定义滚动，滚动元素需添加sg-scroll属性

可选值：vertical、vertical_stop、vertical_base、horizontal、horizontal_stop、horizontal_base、normal
stop表示子节点滚动完后不可冒泡至父元素滚动
base表示根据当前手势方式，判断是否进行当前滚动还是进入父级滚动

可绑定sgScroll事件
```
